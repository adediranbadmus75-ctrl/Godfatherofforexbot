services:
  - type: worker
    name: telegram-member-monitor
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: YOUR_USER_ID
        sync: false
