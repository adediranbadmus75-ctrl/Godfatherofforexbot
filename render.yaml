services:
  - type: worker
    name: telegram-member-monitor
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: USER_ID
        sync: false
      - key: CHANNEL_ID
        sync: false
